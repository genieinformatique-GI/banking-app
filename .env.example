# Database
DATABASE_URL=postgresql://postgres:VOTRE_MOT_DE_PASSE@localhost:5432/blockchain_bank

# JWT
JWT_SECRET=un-secret-jwt-fort-minimum-64-caracteres-obligatoire

# Email Service
RESEND_API_KEY=re_VOTRE_CLE_RESEND

# Server
PORT=3000
NODE_ENV=production

# Frontend (auto-configured in Docker)
FRONTEND_DIST_PATH=/app/artifacts/bank-of-blockchain/dist/public

# PostgreSQL (for docker-compose)
POSTGRES_DB=blockchain_bank
POSTGRES_USER=postgres
POSTGRES_PASSWORD=VOTRE_MOT_DE_PASSE

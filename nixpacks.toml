[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = [
  "pnpm install"
]

[phases.build]
cmds = [
  "pnpm --filter bank-of-blockchain build",
  "pnpm --filter api-server build"
]

[start]
cmd = "node artifacts/api-server/dist/index.js"

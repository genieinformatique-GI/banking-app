[phases.setup]
nixPkgs = ["...", "nodejs_22"]

[phases.install]
cmds = [
  "npm install --force",
  "npm run build-all"
]

[start]
cmd = "NODE_ENV=production node artifacts/api-server/dist/index.cjs"

[variables]
NODE_ENV = "production"
PORT = "3000"
